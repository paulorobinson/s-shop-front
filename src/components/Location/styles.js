import styled from 'styled-components';
import { COLORS } from '../../styles/colors';

export const LocationComponent = styled.p`
  font-size: 1.4rem;
  padding-bottom: 2rem;

  span {
    color: ${COLORS.BLUE};
  }
`;
